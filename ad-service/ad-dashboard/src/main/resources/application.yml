server:
  port: 7002

spring:
  application:
    name: ad-dashboard

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka/


management:
  endpoints:
    web:
      exposure:
        include: "*"